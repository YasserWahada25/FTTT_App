# Développement sans serveur Eureka (évite Connection refused sur localhost:8761).
# IntelliJ : Active profiles = local   ou   --spring.profiles.active=local
eureka.client.enabled=false
spring.cloud.discovery.enabled=false
